# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (if using actual database instead of memory storage)
DATABASE_URL=postgresql://username:password@localhost:5432/artisanai
MONGO_URI=mongodb://localhost:27017/artisanai

# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Session Configuration
SESSION_SECRET=your_super_secure_session_secret

# Optional: Replit Configuration (automatically provided in Replit environment)
REPLIT_DOMAINS=your-repl-name.replit.app
